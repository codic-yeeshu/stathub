apiVersion: v1
kind: Service
metadata:
  name: stathub-server
  namespace: stathub
  labels:
    app: stathub-server
spec:
  type: ClusterIP
  selector:
    app: stathub-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
